"""Core library for open-deepwiki.

The preferred executable entrypoints remain at the repo root:
- app.py (FastAPI)
- indexer.py (CLI)
- config.py (YAML config)

This package contains the reusable building blocks (parser, RAG, etc.).
"""
